---
description: 基于 Unity 6.3 与 GameFrameX 的 SLG 网络游戏开发规范
alwaysApply: true
---

# Unity6.3 SLG 网络游戏开发规范（基于 GameFrameX）

本规则文件用于约束本项目在 Cursor 中的开发行为，目标是：

- **统一**：统一基于 Unity 6.3 的 SLG 类型网络游戏的架构与代码风格。
- **对齐框架**：严格对齐 `GameFrameX.unity` 及其上游 `UnityGameFramework` 的设计与规范。
- **可维护**：提升代码的可读性、可测试性与可扩展性。

> 项目基础框架：
> - `GameFrameX.unity`：`https://github.com/GameFrameX/GameFrameX.unity`
> - 上游框架 `UnityGameFramework` 文档：`https://deepwiki.com/EllanJiang/UnityGameFramework/1-game-framework-overview`

在本项目中生成的任何代码、架构建议与重构方案，都必须优先遵循 **GameFrameX / UnityGameFramework** 的理念与模块划分。

---

## 1. 总体原则

- **优先使用 GameFrameX 提供的功能**：资源管理、事件系统、流程管理（Procedure）、实体管理（Entity）、UI 管理、配置表、网络模块等，禁止重复造轮子。
- **面向模块与组件编程**：遵循 Unity 组件化 + GameFrameX 模块化设计，避免巨型脚本与全局单例滥用。
- **服务器权威（Server Authoritative）**：
  - 作为 SLG 网络游戏，核心战斗/策略逻辑以服务器数据为准。
  - 客户端以**表现与输入收集**为主，避免在客户端维护权威状态。
- **数据驱动**：
  - 单位、建筑、技能、科技等尽量由配置表（如 GameFrameX/UGF 支持的配置系统）驱动。
  - 禁止将可配置参数硬编码在脚本中。
- **性能优先且可读性兼顾**：
  - 对频繁调用路径（如战斗 Tick、寻路、单位 AI）优先考虑性能与 GC 压力。
  - 非关键路径在不影响体验的前提下优先保持清晰易读。

---

## 2. GameFrameX / UnityGameFramework 使用规范

- **流程（Procedure）**：
  - 游戏主流程（启动、登录、加载、战斗、结算等）必须使用框架的 `Procedure` 系统管理。
  - 禁止用自定义状态机或一堆 Manager 单例来替代框架流程管理。
- **实体（Entity）**：
  - 单位、建筑、投射物等战斗对象应建模为框架的实体（Entity），并通过实体模块统一创建/回收。
  - 严禁在场景中随意 `Instantiate/Destroy` 游戏对象，而不经过框架实体管理或对象池。
- **UI（UI Form）**：
  - UI 必须通过框架 UI 模块打开/关闭，并遵循 UIForm 生命周期。
  - UI 脚本只负责表现与交互，业务逻辑下沉到独立模块或数据层。
- **事件与消息**：
  - 优先使用框架提供的事件系统 / 消息系统，而非随处直接调用其他模块。
  - 禁止在核心层滥用 `SendMessage` / 全局静态事件。
- **资源与配置**：
  - 使用框架的资源管理模块加载场景、Prefab、音效等，禁止直接使用 `Resources.Load` 作为主流程方案。
  - 配置（兵种、数值、战斗参数）使用框架建议的配置表方案，并通过统一的配置访问层读取。
- **网络模块**：
  - 网络收发与协议解析应封装在框架网络模块或其扩展示例之上。
  - 客户端不得在业务代码中直接操作底层 Socket。

当 GameFrameX / UnityGameFramework 的文档或示例与本文件有冲突时，**以框架官方文档与示例为最高优先级**。

---

## 3. C# / Unity 编码规范

本节在参考 `rules` 目录中现有 C# / Unity 规范基础上进一步收敛，统一项目风格。

### 3.1 基础命名规范

- **通用约定**：
  - 类 / 结构体 / 接口：`PascalCase`，如 `BattleSystem`, `IEntityView`.
  - 公有属性 / 方法：`PascalCase`。
  - 私有字段：`m_VariableName`，如 `m_Health`, `m_SpawnInterval`.
  - 常量：`c_ConstantName`，如 `c_MaxUnitCount`.
  - 静态字段：`s_StaticName`。
  - 方法参数：`_argumentName`。
  - 局部 / 临时变量：`camelCase`，如 `currentHealth`, `spawnTimer`.
- **Unity 相关**：
  - MonoBehaviour 脚本名与文件名保持一致。
  - 只在必要时暴露公有字段；其他通过 `[SerializeField] private` + 属性访问。

### 3.2 代码结构与组织

- 使用 `#region` 合理划分：
  - `#region Constants` / `#region Fields` / `#region Properties` / `#region Unity Lifecycle` / `#region Public Methods` / `#region Private Methods`。
- 编辑器专用代码必须包裹在：

```csharp
#if UNITY_EDITOR
// Editor-only code
#endif
```

- 保持单一职责：一个脚本只关注一类功能（如：单位表现、AI 决策、UI 面板逻辑），避免“大杂烩”。

### 3.3 组件与场景使用

- 优先使用 `TryGetComponent` 或缓存的 `GetComponent` 结果，避免频繁查找产生 GC。
- 禁止在运行时使用 `GameObject.Find` / `Transform.Find` 作为主要查找手段。
- 所有可复用对象必须做成 Prefab，并通过框架或对象池统一管理。
- 文本一律使用 TextMeshPro 系列组件。

### 3.4 协程与异步

- 在 Unity 主线程内的时序逻辑优先使用协程；跨帧任务避免 `Update` 中手动计时轮询。
- 对 CPU 密集 / 大量数据处理（如大规模路径规划、战斗模拟采样）优先考虑 Job System / 后台任务，并注意与 GameFrameX 的线程安全约束。

---

## 4. SLG 网络与战斗逻辑规范

- **服务器权威**：
  - 战斗结果、单位属性变更、资源变动等必须以服务器结果为准。
  - 客户端可以本地预测，但必须能在服务器校正时安全回滚或纠偏。
- **确定性与重放**（如项目采用）：
  - 核心战斗逻辑应尽量保持确定性，便于战斗重放、回放与同步调试。
  - 与帧同步 / 指令同步方案有关的逻辑要集中管理，不得分散在 UI 或表现脚本里。
- **分层设计**：
  - 战斗/策略逻辑模块：不依赖具体 UI，仅与数据与事件交互。
  - 表现层（动画、特效、音效）：订阅战斗事件，仅做视觉反馈。
  - 网络层：负责协议收发与状态同步，与 GameFrameX 网络模块对齐。

---

## 5. 资源与性能优化

- **对象池**：
  - 频繁生成/销毁的对象（子弹、特效、临时单位等）必须接入对象池或框架实体复用机制。
- **Draw Call 与渲染**：
  - 合理合批（Batching）、使用贴图图集（Atlas）和共享材质。
  - 对复杂 3D 模型启用 LOD。
- **物理与碰撞**：
  - 使用简化碰撞体，合理设置 Layer 与碰撞矩阵，避免无效碰撞计算。
  - 固定时间步（Fixed Timestep）设置要兼顾精度与性能。
- **内存与 GC**：
  - 避免在 `Update` / 战斗 Tick 中产生临时分配（如 `new List<>`）。
  - 避免在热路径中频繁拼接字符串，使用缓存或 StringBuilder。

---

## 6. 错误处理、日志与调试

- 统一通过项目/框架封装的日志系统输出日志，禁止散落使用 `Debug.Log` 族函数。
- 明确区分：
  - 调试日志：仅在开发/测试环境开启。
  - 警告日志：表示潜在问题，但不会中断流程。
  - 错误日志：表示严重问题，需要记录必要上下文信息。
- 对重要流程（登录、战斗开始/结束、战报生成、关键数值计算）建议保留必要的日志与统计信息，便于线上问题排查。
- 对易出错代码（文件 IO、网络请求、配置解析等）使用 `try-catch`，并结合日志输出关键信息。

---

## 7. 项目结构与协作约定

- 目录结构应与 GameFrameX / UnityGameFramework 示例工程保持一致或高度相似，便于新人快速上手。
- 公共基础模块（工具类、扩展方法、通用组件）集中存放，不得随意散落在业务目录中。
- 提交前自检：
  - 是否遵循本文件命名规则与代码结构。
  - 是否优先使用 GameFrameX / UnityGameFramework 模块，而不是自建轮子。
  - 是否避免在热路径中引入明显的性能隐患。

---

## 8. 在 Cursor 中使用本规则

当在 Cursor 中为本项目编写代码、重构或生成方案时，AI 助手应：

1. **默认认为本项目是基于 GameFrameX.unity 的 Unity 6.3 SLG 网络游戏。**
2. **任何方案必须使用工业级方案。**
3. **任何架构建议都需优先考虑如何落地到 GameFrameX / UnityGameFramework 模块。**
4. **生成的 C# / Unity 代码需满足本文件中约定的命名规范与最佳实践。**
5. **涉及战斗/策略逻辑时，明确区分服务器权威与客户端表现层职责。**

只要本文件存在且被加载，本项目的所有自动化生成与智能补全，都应视本规则为**默认约束**。

